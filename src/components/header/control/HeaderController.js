export const homeClicked = (navigate) => {
  navigate("/");
};

export const StoreClicked = (navigate) => {
  navigate("/store");
};

export const CategoryClicked = (navigate) => {
  navigate("/category");
};

export const AboutClicked = (navigate) => {
  navigate("/about");
};
